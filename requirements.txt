# Django and REST framework
Django==5.0.2
djangorestframework==3.15.1

# TensorFlow for plant disease prediction (CPU version for Render)
tensorflow-cpu==2.16.1
numpy==1.26.4

# Image handling
Pillow==10.3.0

# Deployment dependencies
gunicorn==21.2.0
whitenoise==6.6.0

# Optional but common
requests==2.31.0
psycopg2-binary==2.9.9   # only if using PostgreSQL on Render
